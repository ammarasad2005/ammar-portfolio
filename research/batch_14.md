# Batch 14 — Portfolios 66–70

> Research Round 4, Batch 14 of 20. Five portfolios analyzed.
> Carried-forward open tensions to watch for (after 65 portfolios): scroll-driven section transitions (0 in 65); **sound (0 in 65 — DEFINITIVELY RESOLVED this batch by Kavi Castelo)**; designed loading state fully wired (0 in 65); hero motion that itself tells user where to go next (partial — Atal B13 visible tab strip is strongest signal); haptic-style mobile feedback (still 0).
> Special watch for this batch: Hozaifa Ali's domain (hozi-web3.site) was unreachable via Google DNS — alternate resolvers required; Kavi Castelo is an Angular + multi-CSS-framework portfolio (first corpus Angular instance); Sambhu Babu is niche-specialized (Frappe/ERPNext) with strong local-SEO.

---

## 66. Anurag Zete — https://portfolio-anuragzete.web.app

- **Reachable**: yes (HTTP/2 200; Firebase Hosting; Vite + React + Tailwind SPA with PWA manifest + service worker; multi-page architecture: `/`, `/about`, `/skills`, `/work`, `/projects`, `/contact`)
- **First impression**: A standard dark `rgb(17, 24, 39)` (Tailwind gray-900) React/Tailwind developer portfolio with six colorful blur-orb decorations (blue/yellow/teal/pink/indigo/purple). The hero is a centered "Hi, I'm Anurag Zete 👋 / Software Developer & CS Enthusiast" with social links (GitHub / LinkedIn / LeetCode / email). Reads as a 2024–2026 template-grade portfolio — competent but undifferentiated.
- **Visual hierarchy**: H1 "Hi, I'm Anurag Zete 👋" (72px / 700 / system-ui sans-serif / white on gray-900). 2 H2s (About Me, Featured Projects), 3 H3s (project titles). Standard tail: H1 → H2 → H3 → article. Project cards carry inline "completed" status + date-range metadata above the title — a clean semantic upgrade over typical project cards.
- **Layout composition**: Sticky top nav (Portfolio brand left + Home / Contact / About / Skills / Work / Projects links right). Hero section is `min-h-screen flex items-center justify-center` with centered text. About + Featured Projects sections stacked vertically. No sidebar, no off-canvas. Container max-width ~4xl on About.
- **Typography** (family feel, scale, weight): **System font only** (`ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"`). No custom webfont loaded. H1 72px / 700. Body inherits system-ui. No font pairing, no mono eyebrow. Reads as "didn't pick a font."
- **Color palette** (hex codes if discernible): Background `rgb(17, 24, 39)` (gray-900). Body color `rgb(0, 0, 0)`. Decorative blur orbs: blue `rgb(96, 165, 250)`, yellow `rgb(250, 204, 21)`, teal `rgb(45, 212, 191)`, pink `rgb(236, 72, 153)`, indigo `rgb(99, 102, 241)`, purple `rgb(192, 132, 252)` — all Tailwind defaults at 30% opacity. Six-color pastel blur field is the most-trending 2024–2026 hero cliché.
- **Spacing** (dense / balanced / airy): Balanced. Section padding `py-16`/`py-20`. `max-w-4xl mx-auto` content container.
- **Hero section** (what's in it, layout, motion): Centered hero — H1 "Hi, I'm Anurag Zete 👋" + tagline "Software Developer & CS Enthusiast" + 1-line specialization paragraph ("I specialize in building reliable, scalable, and secure systems — from backend services and databases to modern web applications.") + 4 social links (GitHub, LinkedIn, LeetCode, mail). Motion is limited to 5 standard Tailwind keyframes (blink, bounce, ping, spin, fadeIn). Six decorative blur orbs in the background.
- **Navigation** (sticky? hidden? sidebar? minimal?): Sticky top nav. Brand "Portfolio" + 6 nav items (Home / Contact / About / Skills / Work / Projects) + a button (theme toggle likely). Mobile menu hidden behind a button. Links are simple text labels — no slash-route prefix, no mono eyebrow.
- **Section ordering** (list sections top-to-bottom): Nav → Hero (Hi I'm Anurag) → About Me → Featured Projects (3 articles with status + date range) → "View All Projects" link → Footer "© 2026 Anurag Zete. All rights reserved."
- **Scroll experience** (smooth? parallax? scroll-triggered animations?): Plain native scroll. No scroll-triggered reveals. No parallax. No scroll progress bar. Decorative orbs are static (no scroll-tied motion).
- **Animations & motion** (purposeful? excessive? signature move?): Minimal — 5 Tailwind default keyframes (blink, bounce, ping, spin, fadeIn). No signature move. No scroll-triggered reveals. Project cards appear with fadeIn. Reads as "Tailwind defaults out of the box."
- **Hover interactions**: Standard link/button hovers. Project articles have `onclick` (click-to-expand probably). No custom cursor, no magnetic buttons, no focus rings beyond Tailwind defaults.
- **Background effects** (gradient, grain, particles, 3D, plain?): Six blurred colored orbs (`blur-3xl`, opacity-30) absolutely positioned in the hero background. Tailwind default-tinted blob field — the most common 2024–2026 hero decoration pattern. Not particles, not 3D, not grain.
- **3D elements** (yes/no, what kind): no. Zero canvas, zero WebGL, zero Three.js.
- **Responsiveness perception**: Tailwind responsive classes (`md:flex`, `hidden md:flex`, `grid grid-cols-2 gap-4`). Mobile menu drawer. Looks competently responsive.
- **Performance perception** (loads fast? heavy?): Fast — Vite-built, single JS bundle. PWA-enabled (manifest + service worker). Firebase CDN cache hit. Initial HTML is 1.3 KB. No images blocking LCP (3 imgs total, 18 svgs for icons).
- **Emotional feeling** (calm / energetic / serious / playful / futuristic): Calm + template-generic. Reads as a CS student's first polished Vite/React/Tailwind portfolio.
- **Originality** (1-5, with note): **2/5**. Standard dark-mode Tailwind portfolio with the 6-color-blur-orb hero cliché and system-font typography. The only original move is the project status + date-range metadata — "completed 9/11/2025 - 2/25/2026" — which extends B11/12 project tri-state status with explicit date arithmetic.
- **What works** (2-3 bullets):
  - Project cards include `completed` status + `start/end date range` (e.g., "9/11/2025 - 2/25/2026") inline above the title — a clean upgrade over typical project cards. This is a new variant of B11/12 project tri-state status: **status + lifecycle date range**.
  - PWA manifest + service worker (offline-capable portfolio). First instance in the corpus with a real service worker registration script wired by `vite-plugin-pwa:register-sw`. Most "PWA" portfolios ship a manifest but no SW; Anurag ships both.
  - Multi-page architecture with 6 routes (`/`, `/about`, `/skills`, `/work`, `/projects`, `/contact`) — each route is a distinct view, not a section anchor. Reinforces B1 codified multi-page hybrid.
- **What does NOT work** (2-3 bullets — anti-patterns to avoid):
  - System-font only (`ui-sans-serif, system-ui, sans-serif, ...`) — no typographic identity. Reinforces B1 Krishnanand / B1 Bhushan anti-pattern: OS-default font on H1 undermines any visual concept.
  - Six Tailwind-default-color blur-orb background (blue/yellow/teal/pink/indigo/purple) — the most-trending 2024–2026 hero cliché. Reads as "copied from a tutorial."
  - "Failed to fetch" text rendered in the DOM (visible in About section snapshot) — a broken GitHub-stats widget without a fallback. Reinforces B1 Bhushan "shipping broken third-party widgets" anti-pattern.
- **Notable patterns to consider**:
  - **Project status + date-range metadata on cards** (e.g., "completed 9/11/2025 - 2/25/2026") — first corpus instance of *lifecycle date arithmetic* on project cards. Refines B11/12 project tri-state status. Date range signals duration (5 months for PalmPay, 2.5 months for Banking System, 1.5 months for VibeOn) — a strong credibility signal if dates are accurate.
  - **Vite PWA with registered service worker** (`vite-plugin-pwa:register-sw`) — first corpus instance of a fully-wired SW for offline support. Most portfolios ship a manifest.json but no SW script.
  - **LeetCode link in hero social row** — second corpus instance (after B12 Tuncay's competitive-programming links). Signals competitive-programming credibility (CS-student register).

---

## 67. Sambhu Babu — https://sambhusbabu.com

- **Reachable**: yes (HTTP/2 200; Nuxt 3 on Vercel; `x-powered-by: Nuxt`; SEO meta with `og:title`, `og:image`, `twitter:card`; strict CSP; `permissions-policy: camera=(), microphone=(), geolocation=(), interest-cohort=()`; `referrer-policy: strict-origin-when-cross-origin` — strongest security headers in the corpus so far)
- **First impression**: A **consulting/freelance portfolio** for a Frappe & ERPNext Developer in Alappuzha, Kerala. The title `<title>Sambhu S Babu | Frappe & ERPNext Developer in Alappuzha, Kerala</title>` is location-SEO-optimized (rare in the corpus). Dark `rgb(11, 15, 25)` background (tinted-dark — slight blue cast). Reads as a confident niche-specialist freelancer — closer to B13 Hugo's consulting-portfolio register than B1 Jeremiah's hero-only register.
- **Visual hierarchy**: H1 "Sambhu S Babu – Frappe & ERPNext Developer in Alappuzha" (Outfit 96px / 700 / white). 6 H2s (ABOUT ME / ERPNext/Frappe Developer / EXPERTISE / Engineering Excellence / Why Partner With Me? / Frequently Asked Questions). 15 H3s (4 expertise items, 4 partner-reason items, 6 FAQ items, 1 footer brand). Eyebrow → H2 → H3 + body pattern is consistent. Eyebrow style: `text-sm font-bold uppercase tracking-widest` in indigo-400/blue-400 — the same all-caps colored eyebrow pattern from B1 codified.
- **Layout composition**: Sticky top nav (logo left + 5 nav items + "Let's Talk" CTA right). Hero is `min-h-[90vh]` centered with H1 + tagline + 2 CTAs ("Start a Project" / "View My Work") + "ERPNext freelancer" inline link to `/erpnext-freelancer` (SEO landing page). Sections: Hero / About / Expertise (4 items) / Why Partner With Me? (4 items) / FAQ (6 items) / 3-column footer (NAVIGATION / SERVICES / SOCIAL LINKS).
- **Typography** (family feel, scale, weight): **Single-font Outfit** (Google Fonts) — clean modern geometric sans. H1 96px / 700. Eyebrows uppercase tracking-widest. No serif, no mono. Reads as "modern Nuxt/Tailwind template" (Outfit is the most common Nuxt-template default font).
- **Color palette** (hex codes if discernible): Background `rgb(11, 15, 25)` (tinted-dark with blue cast). Body color `rgb(248, 250, 252)` (slate-50). Section alt background `dark:bg-slate-900/50` (translucent slate). Accent colors: indigo-400 `rgb(129, 140, 248)`, blue-400 `rgb(96, 165, 250)` — two semantic accent colors (indigo for eyebrows, blue for service tags). Same dual-accent pattern as B6 Vinit (mustard + cyan).
- **Spacing** (dense / balanced / airy): Airy. Section padding `py-20` / `py-32`. Content max-width ~7xl.
- **Hero section** (what's in it, layout, motion): Centered hero with 96px Outfit H1, tagline ("Expert Frappe Developer & React Native Specialist based in Alappuzha. I craft high-performance ERP setups and mobile experiences."), 2 CTAs ("Start a Project" → `/contact`, "View My Work" → `/portfolio`), and a small "ERPNext freelancer" link to `/erpnext-freelancer` SEO landing page. `fadeInUp` keyframe motion on hero load (0.8s cubic-bezier(.16,1,.3,1)).
- **Navigation** (sticky? hidden? sidebar? minimal?): Sticky top nav. Logo + 5 nav items (Home / Services / Portfolio / Blog / About) + "Let's Talk" CTA. Mobile menu drawer (standard Nuxt pattern).
- **Section ordering** (list sections top-to-bottom): Nav → Hero → About Me (kicker ABOUT ME + H2 ERPNext/Frappe Developer + 4 paragraphs + 6 social links + "More About My Journey" CTA) → EXPERTISE (4 cards: Frappe & ERPNext / React Native Apps / Modern Web Dev / Technical Consultation) → Why Partner With Me? (4 reasons: Rapid Development / Enterprise Security / Native Performance / Local & Global Support) → Frequently Asked Questions (6 questions) → Footer (3 columns: NAVIGATION / SERVICES / SOCIAL LINKS + Privacy Policy + Terms of Service).
- **Scroll experience** (smooth? parallax? scroll-triggered animations?): Plain native scroll with `fadeInUp` keyframe animations on section load. No parallax, no scroll progress bar, no scroll-triggered reveals beyond CSS animation-delay.
- **Animations & motion** (purposeful? excessive? signature move?): Minimal — 7 keyframes (fadeInUp ×2 component-scoped, bounce, ping, pulse, spin). All Tailwind defaults + 2 custom fadeInUp animations with cubic-bezier(.16,1,.3,1) easing. No signature move.
- **Hover interactions**: Standard Tailwind hover transitions (`hover:text-blue-600`, `hover:bg-...`). CTAs use `group relative` with overflow-hidden reveal animation on hover (group-hover translateX reveal of arrow icon). No custom cursor.
- **Background effects** (gradient, grain, particles, 3D, plain?): Solid tinted-dark `rgb(11, 15, 25)` background. Section variants use `dark:bg-slate-900/50` translucent overlay. No gradient, no particles, no 3D, no grain.
- **3D elements** (yes/no, what kind): no.
- **Responsiveness perception**: Tailwind responsive classes throughout (`md:text-5xl`, `lg:text-left`, `flex-col items-stretch lg:items-center lg:flex-row`). Standard Nuxt 3 + Tailwind responsive grid.
- **Performance perception** (loads fast? heavy?): Fast — Nuxt 3 SSR on Vercel. Vercel cache MISS but should hit CDN on subsequent requests. Strict CSP + permissions-policy. No images blocking LCP (3 imgs total, 0 svgs — Lucide icons via CSS mask). Analytics via Google Tag Manager + Vercel Analytics + Vitals.
- **Emotional feeling** (calm / energetic / serious / playful / futuristic): Serious + commercial. Reads as a **freelance consultant's marketing site**, not a developer's portfolio. Closer to a small business website than a personal site.
- **Originality** (1-5, with note): **3/5**. The location-SEO-optimized title (location + niche specialization), the dedicated `/erpnext-freelancer` SEO landing page, and the explicit "Privacy Policy" + "Terms of Service" legal pages are unusual for a developer portfolio. Otherwise the design is Nuxt/Tailwind template-grade.
- **What works** (2-3 bullets):
  - **Location + niche-specialization in `<title>`** ("Sambhu S Babu | Frappe & ERPNext Developer in Alappuzha, Kerala") — first corpus instance of explicit location + niche in title tag. Strong local-SEO signal. Reinforces B13 Hugo's freelance-consulting register, but with a stronger SEO implementation.
  - **Dedicated `/erpnext-freelancer` SEO landing page** — first corpus instance of a niche-keyword-targeted landing page. The link text in the hero ("ERPNext freelancer") is itself the target keyword. Title on the landing page: "Independent ERPNext Consultant & Freelance Frappe Developer" — different keyword cluster from the homepage.
  - **Privacy Policy + Terms of Service links in footer** — first corpus instance of explicit legal pages on a developer portfolio. Reinforces B13 Dipak's GDPR cookie banner pattern with another legal-compliance signal. Real-world signal that freelance portfolios need legal pages if offering services.
- **What does NOT work** (2-3 bullets — anti-patterns to avoid):
  - **FAQ section without structured data** — 6 FAQ items as plain H3 + paragraph pairs, no JSON-LD `FAQPage` schema. Missed SEO opportunity (Google rich-result eligibility).
  - **No portfolio/projects visible on the homepage** — "View My Work" CTA links to `/portfolio` (a separate page). Reinforces the hero-only-home + content-subpages pattern (B1 codified), but for a freelancer, the homepage lacks any social proof (no logos, no client names, no project thumbnails).
  - **Outfit single-font + standard Tailwind palette** — undifferentiated from any 2024–2026 Nuxt/Tailwind template. Reads as "installed Nuxt SEO template." No typographic identity, no signature color move.
- **Notable patterns to consider**:
  - **Location-SEO-optimized `<title>`** (`{Name} | {Niche} Developer in {City}, {State}`) — first corpus instance. Strong local-search signal. Adopt for any portfolio targeting local freelance clients.
  - **Niche-keyword-targeted SEO landing page** (Sambhu's `/erpnext-freelancer`) — first corpus instance. A dedicated landing page targeting a specific keyword cluster ("Independent ERPNext Consultant" / "Freelance Frappe Developer"), distinct from the homepage's identity ("Sambhu S Babu | Frappe & ERPNext Developer in Alappuzha"). Reinforces B13 Hugo's service-taxonomy-as-architecture with a more SEO-correct implementation.
  - **Explicit Privacy Policy + Terms of Service footer links** — first corpus instance. Real-world signal that freelance portfolios offering services need legal pages.
  - **Strong security headers** (strict CSP, permissions-policy: `camera=(), microphone=(), geolocation=(), interest-cohort=()`, referrer-policy: `strict-origin-when-cross-origin`, X-Content-Type-Options: nosniff, X-Frame-Options: DENY) — strongest security-header stack in the corpus. Most portfolios ship zero security headers. Adopt as table-stakes.
  - **"Let's Talk" verb-CTA in nav** — reinforces B1 codified verb/noun brand-y nav labels.
  - **3-column footer with NAVIGATION / SERVICES / SOCIAL LINKS sub-headings** — reinforces B3 codified footer-as-design-surface pattern with explicit categorization.

---

## 68. Kavi Castelo — https://kavindukokila.netlify.app

- **Reachable**: yes (HTTP/2 200; Netlify Edge; Angular SPA — first corpus instance of Angular; redirects `/` → `/home`; HTML 23 KB pre-JS)
- **First impression**: A maximalist **Angular + Angular Material + Bootstrap 5 + Font Awesome** portfolio for a Sri Lankan full-stack Software Engineer in Matara. The hero H1 "Kavindu Kokila Gayashan" is set in Anton (heavy display sans) at 72px / weight-300 (Anton's own weight, the CSS weight is overridden). Pure white background. The page also features a `<video>` background (background.mp4, 1600×900, autoplay + loop) and **two `<audio>` elements wired to click + typing sound effects** — the **first instance of sound in the entire 65+ portfolio corpus**.
- **Visual hierarchy**: H1 "Kavindu Kokila Gayashan" (Anton 72px / 300 / `rgb(44, 62, 80)` — dark slate on white). **0 H2s and 0 H3s on the home page** — only 1 H1 in the entire document outline. This is the most extreme heading-structure failure since B13 Aditya (which had 0 of all). Section labels are styled `<div>` elements. The 4 hero cards (Home / About / Resume / Projects) use `<button>` + `<a>` elements with "see more" links — no semantic section headings.
- **Layout composition**: Top nav with 13 nav items (Home / About / Resume / Pages-dropdown [Certifications / Achievements / Service / Explore tech🤖 / Gallery😍 / Fantasy facts😄 / References] / Projects / Contact) + theme toggle. Hero is a 4-card grid (clickable cards for Home / About / Resume / Projects) + 8 social-platform links (GitHub / GitLab / LeetCode / GFG / Stack Overflow / Codersrank / Google Developer / "GARDEN"). Multi-page architecture with 10+ subpages.
- **Typography** (family feel, scale, weight): **7+ fonts loaded** — Roboto (Angular Material default), Material Icons, Montserrat, Anton (H1 display), Bebas Neue (display), Fira Code (mono body), Jacquard 12 (decorative), Freehand (script). Body is Fira Code (monospace) per inline CSS `body { font-family: Fira Code, monospace; }`. Most font-diverse portfolio in the corpus — but the diversity reads as kitchen-sink, not curated. H1 72px / 300 (Anton's intrinsic weight). Body 14px Roboto (Angular Material default).
- **Color palette** (hex codes if discernible): Background `rgb(255, 255, 255)` (pure white — B2 anti-pattern unless paired with ultralight display, which Anton arguably provides). Body color `rgb(33, 37, 41)` (Bootstrap dark). H1 color `rgb(44, 62, 80)` (Bootstrap "midnight blue" / `#2C3E50` — Flat UI palette). Body uses CSS variables `--color-primary-200` for selection background (referenced but empty when probed). Color scheme is Flat-UI-era (2014 aesthetic).
- **Spacing** (dense / balanced / airy): Dense. Hero is a 4-card grid with social icons below. Lots of content packed per viewport.
- **Hero section** (what's in it, layout, motion): 4-card grid (Home / About / Resume / Projects) — each card is clickable (`cursor: pointer`, `tabindex`). H1 "Kavindu Kokila Gayashan" in Anton. Video background (`./assets/videos/home/background.mp4`, autoplay + loop, muted=false, paused=true due to browser autoplay-with-sound block). Two `<audio>` elements (click.mp3 ×2) in the DOM. 8 social-platform links below the cards. Animation keyframes: 21 (mostly Font Awesome icon animations + Bootstrap spinners + custom `grain`, `svgFlicker`, `dash-arc1`, `fade-down`, `slideIn`).
- **Navigation** (sticky? hidden? sidebar? minimal?): Top nav with 13 nav items (Pages dropdown expands to 7 sub-items: Certifications / Achievements / Service / Explore tech🤖 / Gallery😍 / Fantasy facts😄 / References). Theme toggle button. Most nav items in corpus (13–20 depending on dropdown state). Emoji-prefixed playful subnav labels (`Explore tech🤖`, `Gallery😍`, `Fantasy facts😄`) — first corpus instance of emoji in nav labels.
- **Section ordering** (list sections top-to-bottom): Home page: Nav → Hero (H1 + 4-card grid + 8 social links + video bg) → (no further semantic sections — content lives on subpages). Multi-page architecture with `/home`, `/about`, `/resume`, `/certifications`, `/achievements`, `/service`, `/explore-tech`, `/gallery`, `/fantasy-facts`, `/references`, `/projects`, `/contact`.
- **Scroll experience** (smooth? parallax? scroll-triggered animations?): Plain native scroll. Video background is fixed/looped (not scroll-tied). 21 keyframes but most are Font Awesome icon-pulse-style or Bootstrap spinner-style — not scroll-driven.
- **Animations & motion** (purposeful? excessive? signature move?): **Excessive** — 21 keyframes total: `progress-bar-stripes`, `spinner-border`, `spinner-grow`, `placeholder-glow`, `placeholder-wave` (Bootstrap), `cdk-text-field-autofill-start/end` (Angular CDK), `grain`, `spin`, `svgFlicker`, `dash-arc1`, `_ngcontent-atj-c1_fade-down`, `_ngcontent-atj-c1_spin`, `_ngcontent-atj-c1_slideIn` (custom Angular-scoped), `fa-beat`, `fa-bounce`, `fa-fade`, `fa-beat-fade`, `fa-flip`, `fa-shake`, `fa-spin` (Font Awesome). Most are framework default animations, not signature motion.
- **Hover interactions**: Cards have `cursor: pointer` and `tabindex` — clickable cards. Audio click sound is wired via Angular `playClickSound()` method (`this.clickSound.nativeElement.play()` — confirmed in main.js bundle). **Click sounds on at least 4 elements**: notification wrapper, projectNavigator, support modal trigger, mailto link, project cards.
- **Background effects** (gradient, grain, particles, 3D, plain?): **Video background** (`background.mp4`, 1600×900, autoplay + loop + muted=false — but paused due to browser autoplay-with-sound policy). Plus a `grain` keyframe animation (likely an SVG/CSS noise overlay). Plus `svgFlicker` (likely the 1 SVG element flickering). Triple background layer (video + grain + svgFlicker) — visually busy.
- **3D elements** (yes/no, what kind): No Three.js/WebGL canvas. However, the main bundle references `app-model-three-d` (an Angular component name suggesting a 3D model viewer — likely Angular Three / model-viewer). 0 canvas elements on home page (the 3D component may live on a subpage).
- **Responsiveness perception**: Bootstrap 5 responsive grid + Angular Material responsive utilities. `<meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=2.5, minimum-scale=1.0">` — `user-scalable=no` is an accessibility anti-pattern (violates WCAG 1.4.4).
- **Performance perception** (loads fast? heavy?): **Heavy** — main Angular bundle `main.e1e38a947f1e018a.js` is **2.13 MB** (compressed; unminified would be much larger). Plus Bootstrap CSS, Font Awesome kit, Google Fonts (Roboto + Material Icons + Montserrat + Anton + Bebas Neue + Fira Code + Jacquard 12 + Freehand = 8 font families). Plus background.mp4 video. Plus 19 imgs. The home page screenshot file sizes are 1.4 MB each (compared to ~50 KB for Vishal Bhangare, ~280 KB for Anurag Zete) — confirming heavy visual payload.
- **Emotional feeling** (calm / energetic / serious / playful / futuristic): Energetic + cluttered + retro. Reads as a 2014–2018-era Flat-UI portfolio migrated to Angular. The video bg + 8 social platforms + 13 nav items + 4 hero cards + emoji nav labels + 7 fonts + click sounds all together feel maximalist.
- **Originality** (1-5, with note): **3/5**. The original move is **fully-wired click + typing sound effects** (first in the corpus). The kitchen-sink tech stack (3 CSS frameworks + 8 fonts + video bg) is a coherent maximalist commitment — like B3 Ghulam (86 keyframes) but committed. Anti-patterns (0 H2/H3, user-scalable=no, 2.1MB JS, 8 fonts) bring it back to 3.
- **What works** (2-3 bullets):
  - **Fully-wired click sound + typing sound** — `playClickSound()` and `playTypingSound()` Angular methods confirmed in the main bundle. `click.mp3` is wired to 4+ elements (notification, projectNavigator, support modal, mailto, project cards). `typing.mp3` is wired to a "command-output" element (typing animation with accompanying keystroke sound). This **definitively resolves the open tension "Sound — 0 in 65"** carried since Round 1, Batch 1.
  - **Video background that respects browser autoplay policy** — `background.mp4` is set to `autoplay + muted=false + loop` but is `paused` (the browser blocks autoplay-with-sound). This is the *correct* implementation pattern: declare intent (autoplay + sound) and let the browser policy handle it (pause until user interaction). The video does not break the page — it just waits.
  - **Multi-page architecture with 10+ subpages** (Home / About / Resume / Certifications / Achievements / Service / Explore tech / Gallery / Fantasy facts / References / Projects / Contact) — most extensive multi-page architecture in the corpus. Each subpage is a distinct Angular route. Refines B1 codified multi-page hybrid with the largest content surface area.
- **What does NOT work** (2-3 bullets — anti-patterns to avoid):
  - **0 H2s and 0 H3s on home page** (only 1 H1) — the most extreme heading-structure failure in the corpus alongside B13 Aditya. Document outline is just `H1` with no sub-sections. Screen readers cannot navigate by section.
  - **`user-scalable=no` + `maximum-scale=2.5`** in viewport meta — WCAG 1.4.4 violation (prevents pinch-zoom). First corpus instance of this accessibility anti-pattern.
  - **2.13 MB main JS bundle** (Angular main.js, unminified even larger) + **8 Google Fonts families loaded simultaneously** (Roboto, Material Icons, Montserrat, Anton, Bebas Neue, Fira Code, Jacquard 12, Freehand) — heaviest font + JS payload in the corpus. LCP would suffer significantly.
  - **3 CSS frameworks simultaneously** (Angular Material + Bootstrap 5 + Font Awesome) — kitchen-sink stack. Reinforces B3 Gunjan's 64-tech-stack-PNGs anti-pattern at the framework level.
- **Notable patterns to consider**:
  - **Fully-wired click + typing sound effects** (Kavi Castelo) — **FIRST corpus instance of sound. Definitively resolves open tension "Sound — 0 in 65."** Angular `playClickSound()` and `playTypingSound()` methods call `.nativeElement.play()` on `<audio>` elements bound via Angular template reference variables (`#clickSound`, `#typingSound`). The click sound is wired to interactive elements (notification, project navigator, support modal, mailto, project cards). The typing sound accompanies a typing-animation effect (command-output div with `innerHTML` binding). The pattern is: **sound as affordance feedback** (click confirms interaction; typing sound accompanies text-reveal animation).
  - **Video background with autoplay+loop, sound-on** (paused by browser policy) — first corpus instance of a portfolio video background with sound intent. The implementation respects browser autoplay-with-sound blocking (video pauses rather than throwing an error).
  - **8-platform social link row** (GitHub / GitLab / LeetCode / GFG / Stack Overflow / Codersrank / Google Developer Profile / "GARDEN") — first corpus instance of Codersrank + Google Developer Profile + GFG in a social row. Most platform-diverse social row in the corpus.
  - **Emoji-prefixed playful nav labels** (`Explore tech🤖`, `Gallery😍`, `Fantasy facts😄`) — first corpus instance of emoji in nav labels. Personality signal, but reads as informal for a senior-portfolio context.
  - **`app-model-three-d` Angular component** (referenced in main bundle) — first corpus instance of an Angular-scoped 3D model viewer component name. The component lives on a subpage (0 canvas on home). Suggests Angular Three or `<model-viewer>` usage for a real 3D model display.
  - **Sri Lankan + Matara location** in meta keywords — first corpus instance of Sri Lanka / Matara location. Extends B13 Hugo (France) location diversity.

---

## 69. Vishal Bhangare — https://vishalbhangare.netlify.app

- **Reachable**: yes (HTTP/2 200; Netlify Edge; Vite + React SPA — likely with CSS modules — `_projects_1de90_30`, `_majorProject_1de90_94` class hashes)
- **First impression**: A student/developer portfolio with **bare "Portfolio" title** (B2 codified anti-pattern — same as Fikri Rozan B4 / Akash B2). Pure white background. The H1 "Hello, I'm Vishal Bhangare." is set in DM Sans 40px / 700 but colored `rgb(197, 200, 201)` — **light gray on white** — a contrast failure for the page's primary heading. Reads as a 2022-era student portfolio.
- **Visual hierarchy**: H1 "Hello, I'm V i s h a l B h a n g a r e ." (DM Sans 40px / 700 / `rgb(197, 200, 201)` — light gray; with letter-spacing wave animation). **0 H2s, 0 H3s, 0 H4s** — only 1 H1 on the entire page. All section labels (About, Skills, Education, My Projects, Contact) are styled `<div>` elements. Most extreme heading-structure failure alongside B13 Aditya (0 of all) and B14 Kavi Castelo (1 H1 + 0 H2/H3).
- **Layout composition**: Top nav (Home / About / Projects / Contact — only 4 items, minimalist). Hero: H1 + "A Student and developer." tagline + 2 CTAs (Resume / View More →). About: paragraph + email + 6 social links. Skills: text-list categorized (Core / Frontend / Backend / Database). Education: BSC + MSC cards with CGPA. Projects: 14+ project cards with Live / Source links. Contact: 4-field form (Name / Email / Subject / Message) + Send button. Footer: email repeat.
- **Typography** (family feel, scale, weight): **Single-font DM Sans** (Google Fonts). H1 40px / 700. Body DM Sans. No serif, no mono. Most-modest font choice in the batch (less maximalist than Kavi, less curated than Sambhu).
- **Color palette** (hex codes if discernible): Background `rgb(255, 255, 255)` (pure white). Body color `rgb(33, 53, 71)` (dark slate). H1 color `rgb(197, 200, 201)` (light gray — **WCAG AA contrast failure** at 4.5:1 ratio; the contrast ratio of `rgb(197,200,201)` on `rgb(255,255,255)` is ~1.7:1, well below 4.5:1 AA threshold). 1 SVG icon. No accent color — links inherit slate.
- **Spacing** (dense / balanced / airy): Dense. 14+ project cards stacked vertically with Live/Source link rows.
- **Hero section** (what's in it, layout, motion): Centered or left-aligned hero — H1 "Hello, I'm Vishal Bhangare." (with wave-text keyframe animation, letter-by-letter spacing wave — H1 reads as "V i s h a l B h a n g a r e ." in the DOM) + "A Student and developer." paragraph + Resume link + "View More →" link. Wave animation is the only motion.
- **Navigation** (sticky? hidden? sidebar? minimal?): Top nav with 4 items (Home / About / Projects / Contact). Most minimalist nav in the batch alongside Kavi's 13-item maximalist nav. No CTA button.
- **Section ordering** (list sections top-to-bottom): Nav → Hero (H1 + tagline + 2 CTAs) → About (paragraph + email + 6 socials + "Mail me" + "Follow me on" + "Some other profiles on" + Skills categorized text lists for Core / Frontend / Backend / Database) → Education (BSC Computer Science at B.K. Birla College 2021–2024 CGPA 9.50 / MSC Computer Science 2024–2026 CGPA NA) → My Projects (14+ cards with Live/Source links + View All button) → Contact form (4 fields + Send button) → Footer (email repeat).
- **Scroll experience** (smooth? parallax? scroll-triggered animations?): Plain native scroll. No parallax, no scroll progress bar, no scroll-triggered reveals. Wave-text animation on H1 is the only motion.
- **Animations & motion** (purposeful? excessive? signature move?): Minimal — 3 keyframes total: `_wave-text_x7nif_1` (CSS-module-scoped wave animation on H1 letters), `_fadeinout_1de90_1` (project card fade-in/out), `_hideAnimation_1l3yo_1` (toast/notification hide). Wave-text on the H1 is the **signature move** — letter-by-letter horizontal spacing wave (the H1 in DOM reads "V i s h a l B h a n g a r e ." with spaces between every letter). This is a **first corpus instance of wave-text animation on H1** — different from B3 EMF's three-style H1 and B3 letter-by-letter split.
- **Hover interactions**: Project cards have `onclick` (clickable cards). "Live" + "Source" links per card. Standard link hovers. No custom cursor.
- **Background effects** (gradient, grain, particles, 3D, plain?): Pure white. No gradient, no particles, no grain, no 3D. Plainest background in the batch.
- **3D elements** (yes/no, what kind): no.
- **Responsiveness perception**: Standard Vite + React responsive. No explicit responsive utilities detected, but the layout is simple enough to be responsive by default.
- **Performance perception** (loads fast? heavy?): Fast — Vite-built, minimal JS, 16 imgs (project thumbnails). Screenshot file sizes 27–230 KB (smallest in the batch). Pure white background means no LCP image blocking.
- **Emotional feeling** (calm / energetic / serious / playful / futuristic): Calm + unfinished. Reads as a CS student's first portfolio — competent but undifferentiated.
- **Originality** (1-5, with note): **1/5**. Bare "Portfolio" title, 0 meta tags, 0 H2/H3, light-gray H1 on white (contrast failure), 14+ project cards without curation (B1 Bhushan anti-pattern). The wave-text H1 animation is the only original move. Otherwise template-grade.
- **What works** (2-3 bullets):
  - **Wave-text keyframe animation on H1** (letter-by-letter horizontal spacing wave — H1 in DOM reads "V i s h a l B h a n g a r e ." with spaces) — first corpus instance of wave-text on H1. Different from B3 EMF's three-style H1 and B3 letter-by-letter split. The wave animation creates a "breathing" rhythm on the name — could be adopted as a subtle hero signature if tuned carefully.
  - **Education section with CGPA** (BSC CS CGPA 9.50, MSC CS CGPA NA) — student-portfolio register. CGPA-as-credibility is appropriate for a student. Different from B13 Aditya's "educational grades for senior engineer" anti-pattern (here it's appropriate because Vishal IS a student).
  - **4-item minimalist nav** (Home / About / Projects / Contact) — most minimalist nav in the batch. Reinforces B1 codified minimal-nav pattern.
- **What does NOT work** (2-3 bullets — anti-patterns to avoid):
  - **Bare "Portfolio" title** — anti-pattern #21 (round-2 codified). Reinforces B4 Fikri / B2 Akash. The tab is the first thing recruiters see — leaving it as "Portfolio" signals the portfolio is unfinished.
  - **0 meta tags** (no description, no keywords, no author, no OG, no twitter:card) — first corpus instance of zero meta tags. Page has no SEO surface at all.
  - **0 H2s and 0 H3s** — only 1 H1 in the entire document outline. Document outline is just `H1` followed by paragraphs. Most extreme heading-structure failure alongside B13 Aditya and B14 Kavi.
  - **H1 colored `rgb(197, 200, 201)` on pure white** — light gray on white = ~1.7:1 contrast ratio, well below WCAG AA 4.5:1. The page's primary heading is *barely visible* against the background. Most extreme contrast failure in the corpus.
  - **14+ project cards with Live/Source links** — no curation. Reinforces B1 Bhushan "dumping every project without curation" anti-pattern. Three excellent projects beat fourteen decent ones.
- **Notable patterns to consider**:
  - **Wave-text keyframe on H1** (`V i s h a l B h a n g a r e .` letter-spacing wave) — first corpus instance. Subtle horizontal spacing pulse on each letter. Could be adopted as a hero signature if tuned to be subtle (e.g., 0.5s wave, 2px letter-spacing delta).
  - **CGPA in education section for student portfolios** — appropriate student-portfolio credibility signal (Vishal: 9.50 / NA). Different register from B13 Aditya's inappropriate senior-engineer CGPA listing.
  - **`mailto:` link in footer as primary contact** (vishalbhangare@outlook.in appears 2× on the page) — reinforces B6 Milan's plain-text senior-consultant contact pattern at student register.

---

## 70. Hozaifa Ali — https://hozi-web3.site

- **Reachable**: **NO** — the domain `hozi-web3.site` has expired and is now a **Namecheap parking page**. DNS resolution is inconsistent: Google DNS (8.8.8.8) and Quad9 (9.9.9.9) return SERVFAIL; Cloudflare DNS (1.1.1.1) returns A records `72.251.11.93` and `72.251.11.125`. HTTPS handshake is broken (server doesn't serve TLS on port 443 for this host). HTTP (port 80) returns a 200 OK with a Namecheap parking-page HTML response (~2.8 KB) — `<title></title>` (empty), `<h1></h1>` (empty), `<p>has been recently registered with namecheap.com</p>`, plus Namecheap Auctions app footer with App Store + Google Play badges. The Wayback Machine has 2 archived snapshots (July 15, 2025) but they're not serving content via curl (empty response after 60s timeout); agent-browser also failed to load the archived URL (CDP timeout).
- **First impression**: Unreachable. The portfolio is gone. Cross-referenced via GitHub API: `github.com/hozaifa-ali/personal-website` repo has `homepage: "https://hozi-web3.site/"`, `description: "nothing"` (last updated 2026-07-01, 2 days before this research). The README.md confirms the original was a **React 18 + TypeScript + Vite + Tailwind CSS + Three.js (React Three Fiber) + Framer Motion + Embla Carousel** portfolio with glass-morphism effects, 3D animations, smooth transitions, GitHub-repo integration, contact form, and "About section highlighting Software Engineering background from UET Lahore." Component architecture: Hero.tsx, About.tsx, Projects.tsx, Contact.tsx, Navigation.tsx, Scene3D.tsx — first corpus instance of a `Scene3D` component filename.
- **Visual hierarchy**: N/A (unreachable).
- **Layout composition**: N/A (unreachable). Per GitHub README: Hero / About / Projects / Contact component architecture with a dedicated `Scene3D.tsx` 3D component.
- **Typography** (family feel, scale, weight): N/A (unreachable). Per README stack: Tailwind CSS default system stack (no custom font specified in README).
- **Color palette** (hex codes if discernible): N/A (unreachable). Per README: "Modify colors in `tailwind.config.js`" — no specific palette documented.
- **Spacing** (dense / balanced / airy): N/A (unreachable).
- **Hero section** (what's in it, layout, motion): N/A (unreachable). Per README: "Modern UI with 3D effects and animations", "Smooth scroll animations using Framer Motion", "Glass morphism effects", dedicated `Scene3D.tsx` component. Suggests a Three.js scene in the hero — would have been a candidate for the open tension "Real 3D scene" if reachable.
- **Navigation** (sticky? hidden? sidebar? minimal?): N/A (unreachable). Per README: dedicated `Navigation.tsx` component.
- **Section ordering** (list sections top-to-bottom): N/A (unreachable). Per README component list: Hero → About → Projects → Contact, with Scene3D likely in hero.
- **Scroll experience** (smooth? parallax? scroll-triggered animations?): N/A (unreachable). Per README: "Smooth scroll animations using Framer Motion" — suggests scroll-triggered Framer Motion variants.
- **Animations & motion** (purposeful? excessive? signature move?): N/A (unreachable). Per README: Three.js 3D + Framer Motion animations + Embla Carousel for projects.
- **Hover interactions**: N/A (unreachable).
- **Background effects** (gradient, grain, particles, 3D, plain?): N/A (unreachable). Per README: "3D effects" + "glass morphism" — likely a 3D scene in the hero background.
- **3D elements** (yes/no, what kind): **N/A (unreachable)** — but **per README, would have been YES**: React Three Fiber (Three.js) via dedicated `Scene3D.tsx` component. First corpus instance of a `Scene3D.tsx` component filename. This **would have been a candidate** for resolving the "Real 3D scene" open tension (only Whilmar's WebGL2 particle field and Dale's WebGL2 fluid had touched 3D-adjacent territory as of batch 13). Unverifiable due to domain expiry.
- **Responsiveness perception**: N/A (unreachable). Per README: "Fully responsive design".
- **Performance perception** (loads fast? heavy?): N/A (unreachable). Per README stack: React 18 + Vite + Three.js + Framer Motion — would have been heavier than average.
- **Emotional feeling** (calm / energetic / serious / playful / futuristic): N/A (unreachable).
- **Originality** (1-5, with note): **N/A (unreachable)**. Per README, the *intended* portfolio was a React + TS + Tailwind + Three.js portfolio — a common stack but with a dedicated Scene3D component, glass morphism, and Embla Carousel. Would have been a candidate for the "Real 3D scene" open tension. Cannot score — site is gone.
- **What works** (2-3 bullets):
  - **GitHub README as a portfolio blueprint** — the README is a complete project specification (features, tech stack, project structure, customization instructions, environment variables). Even though the live site is gone, the README serves as a recoverable blueprint. Adopt as a best practice: a portfolio's GitHub repo should have a README detailed enough to rebuild the site.
  - **Component-naming convention `Scene3D.tsx`** — first corpus instance of an explicit 3D-scene component filename in a portfolio codebase. Suggests intentional separation of 3D logic from the rest of the app.
  - **Lahore / UET Lahore context** in About section (per README) — first corpus instance of Pakistan / Lahore location. Extends location diversity beyond Sri Lanka (B14 Kavi), France (B13 Hugo), India (multiple), Kerala (B14 Sambhu).
- **What does NOT work** (2-3 bullets — anti-patterns to avoid):
  - **Domain-expired parking page** — first corpus instance of a portfolio whose domain has expired and is now showing a Namecheap parking/auction page. The portfolio's link in the corpus (`hozi-web3.site`) is dead. The anti-pattern is *not* the expiry itself (domains expire for many reasons) but the *consequence*: the portfolio is referenced in `emmabostian/developer-portfolios` as a live example, but it isn't. Cautionary for the corpus itself: link rot is real, and a portfolio's domain is the most fragile part of its deployment.
  - **GitHub repo description set to "nothing"** — when the live site went down, the GitHub repo's description was changed to "nothing" (rather than e.g., "Domain expired — see archived version"). Missed opportunity to communicate status. Reinforces B13 Aniket's `[now retired]` pattern in the negative: if a project is retired, communicate it.
  - **DNS inconsistency across resolvers** — Google DNS returns SERVFAIL, Cloudflare DNS returns A records. Suggests the domain's DNS was partially deprovisioned. Unusual for a fully-expired domain — indicates the domain might be in a grace period or pending-transfer state.
- **Notable patterns to consider**:
  - **Domain-expired parking page as portfolio failure mode** (Hozaifa Ali) — first corpus instance of a portfolio referenced in the corpus whose domain has expired. Adds a new failure mode to the corpus's anti-pattern catalog: **domain-link-rot**. Cautionary for the premium portfolio being built: choose a domain registrar with auto-renew, and never let the domain be the single point of failure (the GitHub repo + README should be the canonical artifact).
  - **README-as-blueprint** (Hozaifa Ali's GitHub README) — even though the site is gone, the README is a complete project spec (features, tech stack, project structure, customization, env vars). Adopt as best practice: the GitHub repo's README should be detailed enough to rebuild the site from scratch, including env var templates and component structure.
  - **`Scene3D.tsx` component filename** — first corpus instance of an explicit 3D-scene component name in a portfolio codebase. Suggests intentional separation of 3D logic from app logic — adoptable naming convention for any portfolio with a hero 3D scene.
  - **React 18 + TypeScript + Vite + Tailwind + Three.js (R3F) + Framer Motion + Embla Carousel** — the canonical 2024–2026 "premium React portfolio" stack. Per README, this was the intended stack. The stack itself is unoriginal (common in 2024–2026 templates), but the inclusion of Embla Carousel for projects (vs typical card grid) is a slight differentiator.

---

## Batch 14 Synthesis

### Patterns that *reinforce* prior findings

- **Multi-page hybrid with sub-routes** — Anurag (6 routes: `/` `/about` `/skills` `/work` `/projects` `/contact`), Sambhu (5 routes: `/` `/services` `/portfolio` `/blog` `/about` `/contact` + `/erpnext-freelancer` SEO landing page), Kavi (10+ routes: `/home` `/about` `/resume` `/certifications` `/achievements` `/service` `/explore-tech` `/gallery` `/fantasy-facts` `/references` `/projects` `/contact`) all reinforce B1 codified multi-page hybrid pattern. Kavi's 10+ subpages is the largest content surface area in the corpus. Vishal is single-page with anchor nav (the only non-multi-page in the batch).
- **Tinted-neutral dark background** — Sambhu (`rgb(11, 15, 25)` tinted-dark with blue cast) and Anurag (`rgb(17, 24, 39)` Tailwind gray-900) both use tinted darks. Kavi uses pure white (B3 codified escape hatch — but paired with Anton heavy display rather than ultralight). Vishal uses pure white. 2/5 tinted-dark, 2/5 pure-white — pure white is more common in batch 14 than batch 13 (1/5 pure white). Pure-white trend is increasing in 2024–2026.
- **Single-font system** — Vishal (DM Sans only) and Sambhu (Outfit only) both reinforce B1 Triet (single Geist Mono) and B13 Dipak (single JetBrains Mono) with sans-serif single-font variants. Two single-font portfolios in this batch alone vs. one across batches 1–13 (Triet B1 + Dipak B13).
- **Plain-text senior-consultant register** — Sambhu's freelance-consulting site (booking CTA / FAQ / services / Privacy Policy / Terms of Service) reinforces B13 Hugo's consulting-portfolio register with stronger SEO implementation. Sambhu's "Let's Talk" verb-CTA reinforces B13 codified plain-text senior-consultant contact pattern.
- **Footer as design surface** — Sambhu's 3-column footer (NAVIGATION / SERVICES / SOCIAL LINKS + Privacy Policy + Terms of Service) reinforces B3 codified footer-as-design-surface with explicit categorization. Anurag's "© 2026 Anurag Zete. All rights reserved." is minimal footer. Kavi and Vishal lack proper footers.
- **All-caps colored eyebrow** — Sambhu's `text-sm font-bold uppercase tracking-widest` in indigo-400/blue-400 (ABOUT ME / EXPERTISE) reinforces B1 codified eyebrow-then-content pattern. Anurag and Vishal skip eyebrows.
- **Multi-platform social row** — Kavi's 8-platform social row (GitHub / GitLab / LeetCode / GFG / Stack Overflow / Codersrank / Google Developer Profile / "GARDEN") extends B1 codified pattern with rare platforms (Codersrank, GFG, Google Developer Profile).
- **Project tri-state status** — Anurag's `completed` + date-range metadata on project cards ("completed 9/11/2025 - 2/25/2026") reinforces B11/12 project tri-state status pattern with **date-range arithmetic**. First corpus instance of explicit project lifecycle date range.
- **Two semantic accent colors** — Sambhu (indigo-400 for eyebrows + blue-400 for service tags) reinforces B6 Vinit (mustard + cyan) dual-accent pattern. Two accents can coexist if they have distinct semantic roles.
- **FAQ-as-pre-contact-filter** — Sambhu's 6 FAQ items (about ERPNext/Frappe services) before the Contact CTA reinforces B13 Hugo's FAQ-as-pre-contact-filter pattern. Second corpus instance.
- **Service taxonomy as consulting-portfolio architecture** — Sambhu's 4 expertise categories (Frappe & ERPNext / React Native Apps / Modern Web Dev / Technical Consultation) reinforce B13 Hugo's service-taxonomy-as-architecture pattern.
- **Verb/noun nav labels** — Sambhu's "Let's Talk" verb-CTA in nav reinforces B1 codified pattern. Most consulting-portfolio-CTA in the corpus.
- **Privacy Policy + cookie compliance** — Sambhu's "Privacy Policy" + "Terms of Service" footer links reinforce B13 Dipak's GDPR cookie banner pattern with another legal-compliance signal. Real-world signal that freelance/consulting portfolios need legal pages if offering services.
- **System-font-only anti-pattern** — Anurag (`ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"`) reinforces B1 Krishnanand / B1 Bhushan anti-pattern: OS-default font on H1 undermines any visual concept. Vishal's DM Sans-only is also undifferentiated but at least picks a real font.
- **Bare "Portfolio" title anti-pattern** — Vishal's bare "Portfolio" `<title>` reinforces B2 anti-pattern #21 (round-2 codified). Third corpus instance after B4 Fikri and B2 Akash.
- **0 H2s and 0 H3s anti-pattern** — Kavi (1 H1 + 0 H2 + 0 H3) and Vishal (1 H1 + 0 H2 + 0 H3) both reinforce B13 Aditya's most-extreme-heading-failure anti-pattern. Three corpus instances now (B13 Aditya + B14 Kavi + B14 Vishal).
- **Particle.js / multi-color-blur-orb hero cliché** — Anurag's 6 Tailwind-default-color blur orbs (blue/yellow/teal/pink/indigo/purple at 30% opacity) reinforces B13 Dipak's "particle.js / lime-on-black AI-startup palette cliché" — the 2024–2026 hero-decoration cliché is shifting from particle.js to multi-color-blur-orbs.

### Patterns that *contradict* or refine earlier findings

- **Sound — DEFINITIVELY RESOLVED** (Kavi Castelo) — **CONTRADICTS the assumption carried since Round 1, Batch 1 that "Sound — 0 in N portfolios" is an unsolvable tension.** Kavi Castelo's Angular main bundle (verified by `grep -oc "click.mp3" main.js` = 1 occurrence, plus 4 inline Angular template bindings to `["src","./assets/sounds/click.mp3"],["clickSound",""]`) confirms **fully-wired click sound effects** via Angular `playClickSound()` method (`this.clickSound.nativeElement.play()`). A second sound file `typing.mp3` is wired via `playTypingSound()` to a "command-output" element with `innerHTML` binding — a typing-animation-with-sound effect. The sound is functional (wired to interactive elements), not decorative. This **definitively resolves the open tension "Sound"** carried since Round 1. The pattern is: **sound as affordance feedback** (click confirms interaction; typing sound accompanies text-reveal animation). Different from the implied "background music" assumption in earlier batches — sound here is *event-driven*, not ambient.
- **`user-scalable=no` viewport meta anti-pattern** (Kavi Castelo) — **REFINES the implicit accessibility assumption** that portfolios don't ship `user-scalable=no`. Kavi's `<meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=2.5, minimum-scale=1.0">` is the **first corpus instance** of WCAG 1.4.4 violation (prevents pinch-zoom). Cautionary: never disable user-scalable — accessibility failure.
- **Multi-CSS-framework stack** (Kavi Castelo: Angular Material + Bootstrap 5 + Font Awesome kit) — **REFINES B3 Gunjan's "64-tech-stack-PNGs" anti-pattern at the framework level**. Three CSS frameworks simultaneously is the framework-level equivalent of logo soup. Each framework pulls in its own design system (Material elevation + Bootstrap grid + Font Awesome icons) — they conflict visually. Most framework-diverse portfolio in the corpus.
- **8-font-family stack** (Kavi Castelo: Roboto + Material Icons + Montserrat + Anton + Bebas Neue + Fira Code + Jacquard 12 + Freehand) — **REFINES B3 codified "two-font developer-coded feel" pattern** with the *kitchen-sink opposite extreme*. Most font-diverse portfolio in the corpus. Reads as maximalism, not curation. Reinforces commitment-vs-kitchen-sink (B3 codified) at the font-system level.
- **`Scene3D.tsx` component filename** (Hozaifa Ali — per GitHub README) — **REFINES the "Real 3D scene" open tension** with a codebase-level signal. Even though the live site is unreachable, the README confirms a dedicated `Scene3D.tsx` component — first corpus instance of an explicit 3D-scene component name. Reinforces B1 Karan's "real Three.js scene" pattern at the *architectural* level (separate component file vs inline 3D).
- **Wave-text keyframe on H1** (Vishal Bhangare: "V i s h a l B h a n g a r e ." letter-spacing wave) — **REFINES B3 EMF's three-style H1 and B3 letter-by-letter split H1** with a *dynamic letter-spacing pulse* variant. First corpus instance. Different from B3 EMF (regular + bold + italic-underlined keyword static styling) — here the wave is a *time-varying* letter-spacing animation. Subtle horizontal spacing pulse on each letter. Could be adopted as a hero signature if tuned carefully.
- **DNS-inconsistent domain expiry** (Hozaifa Ali) — **REFINES B1 Vinay's "deployed portfolio that doesn't deploy (404)" anti-pattern** with a *partial-DNS-deprovisioning* variant. Google DNS returns SERVFAIL, Cloudflare DNS returns A records — indicates the domain is in a grace period or pending-transfer state, not fully expired. More nuanced than a simple 404.
- **Pure white increasing in 2024–2026** (Kavi + Vishal both pure white) — **REFINES B2 codified "tinted-neutral background, never pure"** with the observation that pure-white is *increasing* in frequency. 2/5 pure-white in batch 14 vs. 1/5 in batch 13. Trend: pure-white is the new default (especially with student/junior portfolios using Vite + Tailwind templates). The codified rule "tinted-neutral unless paired with ultralight display type" still holds — neither Kavi (Anton heavy) nor Vishal (DM Sans 700) qualifies for the escape hatch.
- **Heavy main JS bundle (2.13 MB) on Angular portfolio** (Kavi) — **REFINES B3 Ghulam's "86 keyframes" anti-pattern at the bundle-size level**. Kavi's 2.13 MB main.js (unminified even larger) is the heaviest single JS bundle in the corpus. Angular's bundle size is a known tradeoff — but 2.13 MB is at the high end even for Angular. Plus 8 Google Fonts families = the heaviest font payload in the corpus.
- **2/5 portfolios have proper heading structure** (only Sambhu has H1+H2+H3; Anurag has H1+H2+H3 but only 2 H2s; Kavi and Vishal have 0 H2/H3; Hozaifa is unreachable) — **REFINES the implicit assumption that heading structure is a baseline quality**. 2/5 reachable portfolios have catastrophic heading-structure failures (0 H2/H3). The corpus-wide failure rate is increasing — earlier batches had ~1/5 with heading failures; batch 14 has 2/4 reachable.

### New patterns unique to this batch

- **Fully-wired click + typing sound effects** (Kavi Castelo) — **FIRST corpus instance of sound. DEFINITIVELY RESOLVES open tension "Sound — 0 in 65."** Angular `playClickSound()` and `playTypingSound()` methods call `.nativeElement.play()` on `<audio>` elements bound via Angular template reference variables. Sound wired to interactive elements (notification, project navigator, support modal, mailto, project cards) and a typing animation (command-output div with `innerHTML` binding). Pattern: **sound as affordance feedback** (click confirms interaction; typing sound accompanies text-reveal). Two sound files: `click.mp3` (4+ wirings) and `typing.mp3` (1 wiring).
- **Project status + date-range metadata** (Anurag Zete: "completed 9/11/2025 - 2/25/2026") — first corpus instance of explicit project lifecycle date range on project cards. Refines B11/12 project tri-state status with *duration arithmetic* — 5 months for PalmPay, 2.5 months for Banking System, 1.5 months for VibeOn. Date range signals duration credibility if dates are accurate.
- **Vite PWA with registered service worker** (Anurag Zete — `vite-plugin-pwa:register-sw`) — first corpus instance of a fully-wired service worker for offline support. Most portfolios ship a manifest.json but no SW script. Anurag ships both.
- **Location-SEO-optimized `<title>`** (Sambhu Babu: "Sambhu S Babu | Frappe & ERPNext Developer in Alappuzha, Kerala") — first corpus instance of explicit location + niche in title tag. Strong local-SEO signal.
- **Niche-keyword-targeted SEO landing page** (Sambhu Babu's `/erpnext-freelancer`) — first corpus instance. A dedicated landing page targeting a specific keyword cluster ("Independent ERPNext Consultant" / "Freelance Frappe Developer"), distinct from the homepage's identity. Reinforces B13 Hugo's service-taxonomy-as-architecture with a more SEO-correct implementation.
- **Privacy Policy + Terms of Service footer links** (Sambhu Babu) — first corpus instance of explicit legal pages on a developer portfolio. Reinforces B13 Dipak's GDPR cookie banner with another legal-compliance signal.
- **Strongest security-header stack in corpus** (Sambhu Babu: strict CSP + permissions-policy: `camera=(), microphone=(), geolocation=(), interest-cohort=()` + referrer-policy: strict-origin-when-cross-origin + X-Content-Type-Options: nosniff + X-Frame-Options: DENY + X-XSS-Protection: 1; mode=block) — first corpus instance of full security-header stack. Most portfolios ship zero security headers.
- **Wave-text keyframe on H1** (Vishal Bhangare: "V i s h a l B h a n g a r e ." letter-spacing wave) — first corpus instance of wave-text animation on H1. Different from B3 EMF's three-style H1 and B3 letter-by-letter split — here the wave is a time-varying letter-spacing animation, not static styling.
- **Video background with autoplay+sound (paused by browser policy)** (Kavi Castelo: `background.mp4` 1600×900, autoplay + muted=false + loop, paused=true) — first corpus instance of a portfolio video background with sound intent. Implementation respects browser autoplay-with-sound blocking (video pauses rather than throwing an error). Pattern: declare intent (autoplay + sound) and let the browser policy handle it.
- **Triple background layer** (Kavi Castelo: video bg + `grain` keyframe + `svgFlicker` keyframe) — first corpus instance of three simultaneous background layers. Visually busy.
- **Multi-platform social row with Codersrank + Google Developer Profile** (Kavi Castelo: 8 platforms including GitHub / GitLab / LeetCode / GFG / Stack Overflow / Codersrank / Google Developer Profile / "GARDEN") — first corpus instance of Codersrank + Google Developer Profile + GFG in a social row.
- **Emoji-prefixed playful nav labels** (Kavi Castelo: `Explore tech🤖`, `Gallery😍`, `Fantasy facts😄`) — first corpus instance of emoji in nav labels. Personality signal, but reads as informal for a senior-portfolio context.
- **`app-model-three-d` Angular component** (Kavi Castelo — referenced in main bundle) — first corpus instance of an Angular-scoped 3D-model-viewer component name. Suggests Angular Three or `<model-viewer>` usage for a real 3D model display.
- **Sri Lankan + Matara location** (Kavi Castelo — meta keywords) — first corpus instance of Sri Lanka / Matara location.
- **Lahore / UET Lahore context** (Hozaifa Ali — per GitHub README) — first corpus instance of Pakistan / Lahore location (unverifiable due to domain expiry).
- **Domain-expired parking page** (Hozaifa Ali — `hozi-web3.site` is now a Namecheap parking page) — **first corpus instance of a portfolio whose domain has expired.** Adds a new failure mode to the corpus's anti-pattern catalog: **domain-link-rot**. Cautionary for the corpus itself: link rot is real, and a portfolio's domain is the most fragile part of its deployment.
- **GitHub README-as-blueprint** (Hozaifa Ali — README has complete project spec: features, tech stack, project structure, customization, env vars) — first corpus instance of a recoverable blueprint. Even though the site is gone, the README is detailed enough to rebuild. Adopt as best practice.
- **`Scene3D.tsx` component filename** (Hozaifa Ali — per GitHub README) — first corpus instance of an explicit 3D-scene component filename in a portfolio codebase. Suggests intentional separation of 3D logic from app logic.
- **CGPA in education section for student portfolios** (Vishal Bhangare: BSC CS CGPA 9.50 / MSC CS CGPA NA) — first corpus instance of CGPA in education section. Appropriate for student register (contrast with B13 Aditya's inappropriate senior-engineer CGPA listing).
- **3-column footer with NAVIGATION / SERVICES / SOCIAL LINKS sub-headings** (Sambhu Babu) — first corpus instance of explicit 3-column categorization with H4 sub-headings in footer. Reinforces B3 codified footer-as-design-surface with structural categorization.
- **`text-sm font-bold uppercase tracking-widest` eyebrow style** (Sambhu Babu — `ABOUT ME` / `EXPERTISE` in indigo-400/blue-400) — refines B1 codified eyebrow pattern with explicit Tailwind utility classes. The eyebrow style is now codified as: `text-sm` (12–14px) + `font-bold` + `uppercase` + `tracking-widest` (letter-spacing ~0.1em) + colored (indigo or blue). Most common eyebrow style in the corpus.
- **LeetCode in hero social row** (Anurag Zete — alongside GitHub / LinkedIn / email) — second corpus instance (after B12 Tuncay). Signals competitive-programming credibility (CS-student register).
- **DNS-inconsistent domain expiry** (Hozaifa Ali — Google DNS SERVFAIL, Cloudflare DNS A records) — first corpus instance of partial-DNS-deprovisioning domain expiry. More nuanced than simple 404.

### Updated answers to open tensions

| Tension | Status after batch 14 (70 portfolios) |
|---|---|
| **Sound** | **✅ DEFINITIVELY RESOLVED — first corpus instance in 70 portfolios.** Kavi Castelo (kavindukokila.netlify.app) has fully-wired click sound (`click.mp3`) + typing sound (`typing.mp3`) via Angular `playClickSound()` and `playTypingSound()` methods. Sound is wired to 4+ interactive elements (notification, project navigator, support modal, mailto, project cards) and a typing-animation effect (command-output div). Pattern: **sound as affordance feedback** (click confirms interaction; typing sound accompanies text-reveal animation). Event-driven, not ambient. Different from the implied "background music" assumption in earlier batches. The implementation respects browser autoplay-with-sound policy (video bg pauses rather than erroring). |
| Scroll-driven section transitions | **STILL unsolved. 0 in 70.** None of the 5 portfolios in batch 14 implement real section morphing on scroll. Anurag uses plain native scroll. Sambhu uses plain native scroll + `fadeInUp` keyframes (not scroll-tied). Kavi uses plain native scroll + 21 keyframes (none scroll-tied). Vishal uses plain native scroll + wave-text keyframe (not scroll-tied). Hozaifa is unreachable. Watch continues. |
| Designed loading state (fully wired) | **STILL unsolved. 0 fully-wired in 70.** No portfolio in batch 14 has a fully-wired preloader. Kavi has a `spinner` keyframe (Bootstrap) and `placeholder-glow` + `placeholder-wave` (Bootstrap loading placeholders), but these are framework defaults, not a designed loading state. Watch continues. |
| Hero motion that tells user where to go next | **STILL partially resolved — no further progress this batch.** Anurag has 2 hero CTAs (Resume / View More →) but no orienting motion. Sambhu has 2 hero CTAs ("Start a Project" / "View My Work") but no orienting motion. Kavi has 4 hero cards (Home / About / Resume / Projects) which is *closer* to orienting — the 4-card grid tells the user "you can go to these 4 places" — but it's a static grid, not motion. Vishal has 2 hero CTAs (Resume / View More →) but no orienting motion. Hozaifa is unreachable. Atal B13's visible slash-route tab strip remains the strongest signal. Watch continues. |
| Haptic-style mobile feedback | **STILL unsolved. 0 in 70.** No portfolio in batch 14 implements `navigator.vibrate()` or haptic-style mobile feedback. Watch continues. |

### Anti-patterns flagged this batch

- **Bare "Portfolio" title** (Vishal Bhangare) — reinforces B2 anti-pattern #21. Third corpus instance.
- **0 H2s and 0 H3s on home page** (Kavi Castelo, Vishal Bhangare) — reinforces B13 Aditya anti-pattern. Two more corpus instances. Most extreme heading-structure failures in the corpus alongside B13 Aditya.
- **0 meta tags** (Vishal Bhangare — no description, no keywords, no author, no OG, no twitter:card) — first corpus instance of zero meta tags. Page has no SEO surface at all.
- **H1 colored `rgb(197, 200, 201)` on pure white** (Vishal Bhangare) — first corpus instance of catastrophic H1 contrast failure. ~1.7:1 contrast ratio, well below WCAG AA 4.5:1. The page's primary heading is barely visible against the background.
- **`user-scalable=no` + `maximum-scale=2.5`** in viewport meta (Kavi Castelo) — first corpus instance of WCAG 1.4.4 violation (prevents pinch-zoom).
- **System-font-only H1** (Anurag Zete — `ui-sans-serif, system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"`) — reinforces B1 Krishnanand / B1 Bhushan anti-pattern. No typographic identity.
- **6-color Tailwind-default-blur-orb hero background** (Anurag Zete — blue/yellow/teal/pink/indigo/purple at 30% opacity) — most-trending 2024–2026 hero cliché. Reinforces B13 Dipak's "particle.js / lime-on-black AI-startup palette cliché" — the cliché is shifting from particle.js to multi-color-blur-orbs.
- **"Failed to fetch" rendered in DOM** (Anurag Zete — broken GitHub-stats widget without fallback) — reinforces B1 Bhushan "shipping broken third-party widgets" anti-pattern.
- **3 CSS frameworks simultaneously** (Kavi Castelo: Angular Material + Bootstrap 5 + Font Awesome kit) — first corpus instance at framework level. Reinforces B3 Gunjan's 64-tech-stack-PNGs anti-pattern at the framework level.
- **8 Google Fonts families loaded simultaneously** (Kavi Castelo: Roboto + Material Icons + Montserrat + Anton + Bebas Neue + Fira Code + Jacquard 12 + Freehand) — heaviest font payload in the corpus. Kitchen-sink font system.
- **2.13 MB main JS bundle** (Kavi Castelo — Angular main.js) — heaviest single JS bundle in the corpus. Plus the 8 fonts = total page weight well over 3 MB.
- **14+ project cards without curation** (Vishal Bhangare — Live/Source link rows on every card) — reinforces B1 Bhushan "dumping every project without curation" anti-pattern.
- **Domain-expired parking page** (Hozaifa Ali — `hozi-web3.site` is now a Namecheap parking page) — first corpus instance. New failure mode: domain-link-rot. Cautionary for the corpus itself.
- **GitHub repo description set to "nothing"** when the live site went down (Hozaifa Ali) — missed opportunity to communicate status. Reinforces B13 Aniket's `[now retired]` pattern in the negative.
- **FAQ section without structured data** (Sambhu Babu — 6 FAQ items as plain H3 + paragraph, no JSON-LD `FAQPage` schema) — missed SEO opportunity (Google rich-result eligibility).
- **Triple background layer** (Kavi Castelo: video bg + `grain` keyframe + `svgFlicker` keyframe) — visually busy. Reinforces "commitment vs kitchen-sink" at the background-effects level.

### Adoptability recommendations for the premium portfolio being built

- **Adopt (strongly)**: Kavi Castelo's **event-driven sound as affordance feedback** — click sound on interactive elements + typing sound on text-reveal animations. This is the *correct* sound pattern (event-driven, not ambient). Avoid background music. Wire click sounds to primary CTAs (Resume download, project card clicks) and a typing sound to any typewriter-animation effect. Honor browser autoplay-with-sound policy (sound triggers on user interaction, not page load).
- **Adopt (strongly)**: Anurag Zete's **project status + date-range metadata** ("completed 9/11/2025 - 2/25/2026") on project cards. Refines B11/12 project tri-state status with *duration arithmetic*. Signals project lifecycle duration (months) — strong credibility signal if dates are accurate.
- **Adopt (strongly)**: Sambhu Babu's **strong security-header stack** (strict CSP + permissions-policy: `camera=(), microphone=(), geolocation=(), interest-cohort=()` + referrer-policy + X-Content-Type-Options + X-Frame-Options + X-XSS-Protection). Table-stakes for any modern portfolio. Most portfolios ship zero security headers.
- **Adopt (strongly)**: Sambhu Babu's **location-SEO-optimized `<title>`** if targeting local freelance clients (`{Name} | {Niche} Developer in {City}, {State}`). Strong local-search signal.
- **Adopt (strongly)**: Sambhu Babu's **Privacy Policy + Terms of Service footer links** if offering services. Real-world signal that freelance/consulting portfolios need legal pages.
- **Adopt**: Sambhu Babu's **niche-keyword-targeted SEO landing page** (dedicated `/keyword-cluster` page distinct from homepage identity). Adopt for any portfolio with multiple service offerings.
- **Adopt**: Anurag Zete's **Vite PWA with registered service worker** (`vite-plugin-pwa:register-sw`) for offline-capable portfolio. Most portfolios ship a manifest.json but no SW script. The SW is what makes a portfolio truly PWA.
- **Adopt**: Vishal Bhangare's **wave-text keyframe on H1** as a subtle hero signature (if tuned carefully — e.g., 0.5s wave, 2px letter-spacing delta). Different from B3 EMF's three-style H1 and B3 letter-by-letter split — here the wave is a *time-varying* letter-spacing animation.
- **Adopt**: Hozaifa Ali's **README-as-blueprint** pattern — the GitHub repo's README should be detailed enough to rebuild the site from scratch (features, tech stack, project structure, customization, env vars).
- **Adopt**: Hozaifa Ali's **`Scene3D.tsx` component filename** convention — intentional separation of 3D logic from app logic. Applicable to any portfolio with a hero 3D scene.
- **Adopt**: Sambhu Babu's **3-column footer with NAVIGATION / SERVICES / SOCIAL LINKS sub-headings** (with H4 semantic markup) — reinforces B3 codified footer-as-design-surface with structural categorization.
- **Adopt**: Kavi Castelo's **Codersrank + Google Developer Profile** in social row (if applicable) — rare-platform social signals for senior credibility.
- **Adopt**: Kavi Castelo's **video background with autoplay+sound (paused by browser policy)** pattern — declare intent (autoplay + sound) and let the browser policy handle it. The video pauses rather than erroring. Resilient implementation.
- **Adopt**: Sambhu Babu's **FAQ-as-pre-contact-filter** (qualifies leads before they book a call/contact). Second corpus instance after B13 Hugo.
- **Adopt**: Sambhu Babu's **"Let's Talk" verb-CTA in nav** — reinforces B1 codified verb/noun brand-y nav labels.
- **Consider**: Kavi Castelo's **typing sound with command-output animation** — could pair with B3 EMF's typography options panel for a "code-typing sound when adjusting typography" interaction.
- **Consider**: Anurag Zete's **LeetCode in hero social row** if competitive-programming credibility is a target.
- **Consider**: Vishal Bhangare's **CGPA in education section** if positioning as student/junior. (Inappropriate for senior register — see B13 Aditya anti-pattern.)
- **Avoid**: Kavi Castelo's **multi-CSS-framework stack** (3 frameworks simultaneously). Pick one CSS framework and commit.
- **Avoid**: Kavi Castelo's **8-font-family stack**. Pick 1–2 fonts (B1 codified two-font pattern) or 1 (B1 Triet single-font extreme).
- **Avoid**: Kavi Castelo's **`user-scalable=no` viewport meta** — WCAG 1.4.4 violation. Always allow pinch-zoom.
- **Avoid**: Vishal Bhangare's **bare "Portfolio" title** — pick a real title (`{Name} | {Role}` or `{Name} — {Tagline}`).
- **Avoid**: Vishal Bhangare's **0 meta tags** — at minimum ship description, OG:title, OG:image, twitter:card.
- **Avoid**: Vishal Bhangare's **light-gray H1 on white** contrast failure — verify H1 contrast ratio ≥ 4.5:1 (WCAG AA).
- **Avoid**: Anurag Zete's **6-color Tailwind-default-blur-orb hero background** — the most-trending 2024–2026 hero cliché. Use a single accent color or a real signature visual (3D / WebGL / typography).
- **Avoid**: Anurag Zete's **system-font-only typography** — pick a real font.
- **Avoid**: Anurag Zete's **"Failed to fetch" rendered in DOM** — wrap third-party widgets in error boundaries with fallbacks.
- **Avoid**: Vishal Bhangare's **14+ project cards without curation** — curate ruthlessly (3 excellent > 14 decent).
- **Avoid**: Hozaifa Ali's **domain-expired parking page** failure mode — set up auto-renew on the domain registrar, and never let the domain be the single point of failure (GitHub repo + README should be the canonical artifact).

### Strongest portfolio in batch 14

**Sambhu Babu (sambhusbabu.com)** — 3/5 originality. The strongest portfolio in batch 14 and the strongest *freelance-consulting* portfolio in the corpus alongside B13 Hugo. Reference implementation for: location-SEO-optimized title, niche-keyword-targeted SEO landing page (`/erpnext-freelancer`), Privacy Policy + Terms of Service footer links, strongest security-header stack in corpus (strict CSP + permissions-policy + referrer-policy + X-Content-Type-Options + X-Frame-Options + X-XSS-Protection), FAQ-as-pre-contact-filter, and 3-column footer with NAVIGATION/SERVICES/SOCIAL LINKS categorization. The design itself is Nuxt/Tailwind template-grade (Outfit single-font, indigo/blue Tailwind defaults), but the SEO + legal + security implementation is the most production-ready in the corpus. The "ERPNext freelancer" inline link in the hero pointing to `/erpnext-freelancer` is a particularly elegant SEO move — the link text IS the target keyword.

### Weakest portfolio in batch 14

**Vishal Bhangare (vishalbhangare.netlify.app)** — 1/5 originality. A student portfolio undermined by: bare "Portfolio" title (B2 anti-pattern #21), 0 meta tags (first corpus instance), 0 H2s and 0 H3s (only 1 H1 in entire document outline), H1 colored `rgb(197, 200, 201)` light gray on pure white (~1.7:1 contrast ratio, well below WCAG AA 4.5:1 — most extreme H1 contrast failure in corpus), 14+ project cards without curation (B1 Bhushan anti-pattern), and DM Sans single-font with no accent color. The wave-text keyframe on H1 is the only original move. Cautionary artifact — anti-pattern reference for: bare title, zero meta tags, zero heading structure, H1 contrast failure, and uncuration.

### Honorable mention

**Kavi Castelo (kavindukokila.netlify.app)** — 3/5 originality. The most *consequential* portfolio in batch 14 despite being maximalist: **definitively resolves the open tension "Sound — 0 in 65"** carried since Round 1, Batch 1. Fully-wired click sound + typing sound via Angular `playClickSound()` and `playTypingSound()` methods is the first corpus instance of sound as affordance feedback. Also first corpus instance of: video background with autoplay+sound (paused by browser policy), 8-platform social row with Codersrank + Google Developer Profile, `app-model-three-d` Angular 3D component, emoji-prefixed nav labels, and Sri Lankan / Matara location. However, the implementation is anti-pattern-heavy: 0 H2s/H3s, `user-scalable=no`, 2.13 MB JS bundle, 8 fonts, 3 CSS frameworks. The original move (sound) is adoptable; the implementation (kitchen-sink stack) is not.

### Notable failure

**Hozaifa Ali (hozi-web3.site)** — UNREACHABLE. The domain has expired and is now a Namecheap parking page. First corpus instance of a portfolio whose domain has expired. Per GitHub README, the original was a React 18 + TS + Tailwind + Three.js (R3F) + Framer Motion + Embla Carousel portfolio with a dedicated `Scene3D.tsx` component — would have been a candidate for the open tension "Real 3D scene." Unverifiable due to domain expiry. Cautionary for the corpus itself: link rot is real, and a portfolio's domain is the most fragile part of its deployment. The README-as-blueprint is the only artifact that survives the domain expiry.

### Open tensions update summary

- **Sound**: ✅ **DEFINITIVELY RESOLVED — first corpus instance in 70 portfolios.** Kavi Castelo's Angular `playClickSound()` (wired to 4+ interactive elements) + `playTypingSound()` (wired to typing animation) resolves the open tension carried since Round 1, Batch 1. Pattern: **event-driven sound as affordance feedback** (click confirms interaction; typing sound accompanies text-reveal). Not ambient music. Adoptable pattern for the premium portfolio.
- **Scroll-driven section transitions**: STILL 0 in 70. Watch continues.
- **Designed loading state (fully wired)**: STILL 0 in 70. Watch continues.
- **Hero motion that tells user where to go next**: PARTIALLY resolved (Atal B13's slash-route tab strip remains strongest signal). Kavi's 4-card hero grid is a *static* orienting affordance (not motion). No further progress this batch.
- **Haptic-style mobile feedback**: STILL 0 in 70. Watch continues.
